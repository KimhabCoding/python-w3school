# Remove List Items 
ls = ['Phnom Penh', 'Apple', 'Chrome', 'Brave Browser', 'Dell']
# ls.remove('Apple')
# chrome != Chrome 
# l2 = ls.remove('chrome')
# print(ls.remove('Apple')) --> wrong 

# pop(index)
# pop() remove last item 
# ls.pop(0) #['Apple', 'Chrome', 'Brave Browser']
# ls.pop()
# del list(index) = list.pop(index)
# del ls[1] # list.pop(1)
ls.clear()
print(ls)